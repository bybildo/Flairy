<Page x:Class="CoursFlairy.View.Bussiness.PlanePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CoursFlairy.View.Bussiness"
      xmlns:convertersearch="clr-namespace:CoursFlairy.ViewModel.Converter.SearchPage"
      mc:Ignorable="d" Loaded="Page_Loaded"
      d:DesignHeight="650" d:DesignWidth="1040"
      Title="PlanePage">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Border" x:Key="ButtonStyle">
            <Setter Property="Background" Value="{DynamicResource White}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" To="0.6"/>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" To="5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>

                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" To="0.35"/>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" To="4"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="ButtonStyleT">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
        </Style>

        <convertersearch:TextToVisibilityConverter x:Key="TextToVisibilityConverter"/>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ListBoxItemContainerStyle1" TargetType="{x:Type ListBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Grid x:Name="ItemGrid" Margin="2, 1, 2, 30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="{Binding ListItemHeight, RelativeSource={RelativeSource AncestorType={x:Type Page}}}"/>
                                <RowDefinition x:Name="ExpandedRow" Height="0"/>
                            </Grid.RowDefinitions>
                            
                            <Border x:Name="Bd" Grid.Row="0" CornerRadius="10" Background="{DynamicResource White}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" MouseDown="PlaneItem_MouseDown">
                                <Border.Effect>
                                    <DropShadowEffect x:Name="ShadowEffect" BlurRadius="3" ShadowDepth="0.2" Color="{DynamicResource Shadow}" Direction="270" Opacity="0.3" />
                                </Border.Effect>
                            </Border>
                            
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.6*"/>
                                    <ColumnDefinition Width="26*"/>
                                    <ColumnDefinition Width="0.6*"/>
                                    <ColumnDefinition Width="1.4*"/>
                                    <ColumnDefinition Width="0.6*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Grid Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="1.3*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <ScrollViewer MouseEnter="ScrollViewer_MouseEnter" MouseLeave="ScrollViewer_MouseLeave" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden" PreviewMouseWheel="ScrollViewer_PreviewHorizontalMouseWheel">
                                        <TextBlock Text="{Binding PlaneName}" VerticalAlignment="Center" FontFamily="Bahnschrift" Foreground="{DynamicResource MainColor100}" FontSize="24"/>
                                    </ScrollViewer>
                                </Grid>
                                
                                <Grid Grid.Column="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="3*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="1" Background="Transparent" MouseEnter="Seat_MouseEnter" MouseLeave="Seat_MouseLeave">
                                        <Grid.Resources>
                                            <Style TargetType="Path">
                                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                <Setter Property="StrokeThickness" Value="0.7"/>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="180"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Grid.Resources>

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="0*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <Grid  Grid.Column="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="2*"/>
                                                <RowDefinition Height="0.04*"/>
                                                <RowDefinition Height="1.2*"/>
                                            </Grid.RowDefinitions>

                                            <Path Stretch="Uniform" Stroke="{DynamicResource EconomColor}"  Data="{DynamicResource adult 1}"/>
                                            <Viewbox Grid.Row="2">
                                                <TextBlock Text="{Binding EconomySeats, Mode=OneWay}" FontFamily="Bahnschrift"/>
                                            </Viewbox>
                                        </Grid>

                                        <Grid Grid.Column="3">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="2*"/>
                                                <RowDefinition Height="0.04*"/>
                                                <RowDefinition Height="1.2*"/>
                                            </Grid.RowDefinitions>

                                            <Path Stretch="Uniform" Stroke="{DynamicResource BussinessColor}" Data="{DynamicResource adult 2}"/>
                                            <Viewbox Grid.Row="2">
                                                <TextBlock Text="{Binding BusinessSeats, Mode=OneWay}" FontFamily="Bahnschrift"/>
                                            </Viewbox>
                                        </Grid>

                                        <Grid Grid.Column="5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="2*"/>
                                                <RowDefinition Height="0.04*"/>
                                                <RowDefinition Height="1.2*"/>
                                            </Grid.RowDefinitions>

                                            <Path Stretch="Uniform" Stroke="{DynamicResource FirstColor}" Data="{DynamicResource adult 3}"/>
                                            <Viewbox Grid.Row="2">
                                                <TextBlock Text="{Binding FirstSeats, Mode=OneWay}" FontFamily="Bahnschrift"/>
                                            </Viewbox>
                                        </Grid>

                                        <Grid Grid.Column="7">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="2*"/>
                                                <RowDefinition Height="0.04*"/>
                                                <RowDefinition Height="1.2*"/>
                                            </Grid.RowDefinitions>

                                            <Path Stretch="Uniform" Fill="Navy" Data="{DynamicResource seat}"/>
                                            <Viewbox Grid.Row="2">
                                                <TextBlock Text="{Binding AllSeats, Mode=OneWay}" FontFamily="Bahnschrift"/>
                                            </Viewbox>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Grid>
                            
                            <!-- Expandable Cabin Layout Section -->
                            <Border x:Name="CabinLayoutBorder" Grid.Row="1" CornerRadius="0,0,10,10" Background="{DynamicResource White}" BorderBrush="{DynamicResource MainColor20}" BorderThickness="1,0,1,1" Opacity="0" Margin="2,0,2,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="0.2" Color="{DynamicResource Shadow}" Direction="270" Opacity="0.3" />
                                </Border.Effect>
                                <ScrollViewer x:Name="CabinLayoutScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="20" MaxHeight="300" MaxWidth="800">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Header -->
                                        <TextBlock Grid.Row="0" Text="Схема салону" FontFamily="Bahnschrift" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MainColor100}" Margin="0,0,0,15" HorizontalAlignment="Center"/>
                                        
                                        <!-- Cabin Layout with ScrollViewer -->
                                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="250" MaxWidth="750" PreviewMouseWheel="CabinScrollViewer_PreviewMouseWheel">
                                            <Border Background="{DynamicResource EmptyColor}" CornerRadius="8" Padding="15" HorizontalAlignment="Center">
                                                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                                    <Line Stroke="{DynamicResource MainColor100}" StrokeThickness="2" X2="{Binding CabinWidth}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                                                    <ItemsControl ItemsSource="{Binding CabinLayout}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <ItemsControl ItemsSource="{Binding}">
                                                                    <ItemsControl.ItemsPanel>
                                                                        <ItemsPanelTemplate>
                                                                            <UniformGrid Columns="{Binding DataContext.CabinColumns, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"/>
                                                                        </ItemsPanelTemplate>
                                                                    </ItemsControl.ItemsPanel>
                                                                    <ItemsControl.ItemTemplate>
                                                                        <DataTemplate>
                                                                            <Grid Width="{Binding WidthElement}" Height="{Binding HeightElement}" >
                                                                                <Border Margin="7" CornerRadius="7" Background="{Binding PlaneLayoutColor}" Opacity="0.6"/>
                                                                                <Border Margin="7" CornerRadius="7" BorderThickness="2" BorderBrush="{Binding PlaneLayoutColor}"/>

                                                                                <Grid Background="Transparent">
                                                                                    <Grid.RowDefinitions>
                                                                                        <RowDefinition Height="1*"/>
                                                                                        <RowDefinition Height="1.5*"/>
                                                                                        <RowDefinition Height="0.9*"/>
                                                                                    </Grid.RowDefinitions>

                                                                                    <Viewbox Grid.Row="1">
                                                                                        <TextBlock Text="{Binding SeatText}" Foreground="{DynamicResource EmptyColor}" FontFamily="Bahnschrift"/>
                                                                                    </Viewbox>
                                                                                </Grid>

                                                                                <Grid>
                                                                                    <Grid.Resources>
                                                                                        <Style TargetType="Path">
                                                                                            <Setter Property="Stretch" Value="Uniform"/>
                                                                                            <Setter Property="Fill" Value="{DynamicResource EnterColor}"/>
                                                                                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                                                            <Setter Property="Margin" Value="-5"/>
                                                                                            <Setter Property="Visibility" Value="{Binding PathVisiblity}"/>
                                                                                        </Style>
                                                                                    </Grid.Resources>

                                                                                    <Grid.RowDefinitions>
                                                                                        <RowDefinition Height="*"/>
                                                                                        <RowDefinition Height="*"/>
                                                                                        <RowDefinition Height="*"/>
                                                                                        <RowDefinition Height="*"/>
                                                                                    </Grid.RowDefinitions>

                                                                                    <Path Grid.Row="1" Data="{DynamicResource arrow}" RenderTransform="{Binding PathAngleTransform}"/>
                                                                                    <Path Grid.Row="2" Data="{DynamicResource arrow}" RenderTransform="{Binding PathAngleTransform}"/>
                                                                                </Grid>
                                                                            </Grid>
                                                                        </DataTemplate>
                                                                    </ItemsControl.ItemTemplate>
                                                                </ItemsControl>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                    <Line Stroke="{DynamicResource MainColor100}" StrokeThickness="2" X2="{Binding CabinWidth}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </ScrollViewer>
                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ShadowEffect" 
                                                 Storyboard.TargetProperty="Opacity" 
                                                 From="0.3" To="0.6" Duration="0:0:0.25"/>
                                        <DoubleAnimation Storyboard.TargetName="ShadowEffect" 
                                                 Storyboard.TargetProperty="BlurRadius" 
                                                 From="3" To="5" Duration="0:0:0.25"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ShadowEffect" 
                                                 Storyboard.TargetProperty="Opacity" 
                                                 From="0.6" To="0.3" Duration="0:0:0.25"/>
                                        <DoubleAnimation Storyboard.TargetName="ShadowEffect" 
                                                 Storyboard.TargetProperty="BlurRadius" 
                                                 From="5" To="3" Duration="0:0:0.25"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="5, 0, -5,0 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="0, 5, 0,-5 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" Height="{TemplateBinding Height}" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1,0"/>
            <Setter Property="Width" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="MinWidth" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.02*"/>
                                <RowDefinition Height="4*"/>
                                <RowDefinition Height="0.02*"/>
                            </Grid.RowDefinitions>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.Row="1"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="1">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbVertical}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="MinHeight" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="BorderThickness" Value="0,1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.02*"/>
                                        <ColumnDefinition Width="4*"/>
                                        <ColumnDefinition Width="0.02*"/>
                                    </Grid.ColumnDefinitions>
                                    <Track x:Name="PART_Track" Grid.Column="1" IsEnabled="{TemplateBinding IsMouseOver}">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbHorizontal}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanHorizontallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" CanVerticallyScroll="False" Grid.Column="0" Content="{TemplateBinding Content}" CanContentScroll="{TemplateBinding CanContentScroll}" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid x:Name="MainGrid" Background="White" DataContext="{Binding RelativeSource={RelativeSource AncestorLevel=1, AncestorType=Page, Mode=FindAncestor}}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="0.05*"/>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="{DynamicResource MainColor20}" Background="Transparent" BorderThickness="1" Grid.Column="1" Grid.Row="1" CornerRadius="6">
                <Grid Margin="5, 0, 5, 0">
                    <TextBlock Text="Пошук за назвою літака" Grid.Column="1" FontSize="20" Margin="2,0,0,0" Foreground="{DynamicResource HintColor}" Opacity="0.6" VerticalAlignment="Center" Visibility="{Binding ElementName=PlaneSearch, Path=Text, Mode=OneWay, Converter={StaticResource TextToVisibilityConverter}}"/>
                    <ScrollViewer  Grid.Column="1" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden" PanningMode="HorizontalOnly" PreviewMouseWheel="ScrollViewer_PreviewHorizontalMouseWheel">
                        <TextBox Name="PlaneSearch" Foreground="{DynamicResource Black}" Text="{Binding PlaneName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="200" FontFamily="Bahnschrift" FontSize="25"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="5" Grid.Row="1" Style="{StaticResource ButtonStyle}" PreviewMouseDown="AddPlane_MouseDown">
                <Viewbox Margin="8">
                    <TextBlock Text="Додати літак" FontFamily="Bahnschrift" Style="{StaticResource ButtonStyleT}"/>
                </Viewbox>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0.5" Color="{DynamicResource Shadow}" Direction="320" Opacity="0.3" />
                </Border.Effect>
            </Border>
            <Path Grid.Column="3" Grid.Row="1" Stretch="Uniform" Fill="{DynamicResource MainColor100}" Data="{DynamicResource filter}" Margin="0,7,0,7"/>
        </Grid>
        <Line Stroke="Navy" StrokeThickness="1" VerticalAlignment="Bottom" X2="{Binding ElementName=MainGrid, Path=ActualWidth}"/>

        <Grid Grid.Row="1" Name="PlanesGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="0.35*"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="1" Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                <ListBox ItemContainerStyle="{DynamicResource ListBoxItemContainerStyle1}" ItemsSource="{Binding Planes}" Background="Transparent" BorderBrush="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">

                </ListBox>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
