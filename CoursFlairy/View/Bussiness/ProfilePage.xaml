<Page x:Class="CoursFlairy.View.Bussiness.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CoursFlairy.View.Bussiness"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1040"
      Title="ProfilePage">

    <Page.Resources>
        <!-- Card Style -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource White}"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" 
                                    ShadowDepth="2" 
                                    Color="{DynamicResource Shadow}" 
                                    Direction="270" 
                                    Opacity="0.15" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.2" 
                                               Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" 
                                               To="0.25"/>
                                <DoubleAnimation Duration="0:0:0.2" 
                                               Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" 
                                               To="12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.2" 
                                               Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" 
                                               To="0.15"/>
                                <DoubleAnimation Duration="0:0:0.2" 
                                               Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" 
                                               To="8"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Title Text Style -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Card Header Style -->
        <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Stat Number Style -->
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Stat Label Style -->
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource HintColor}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <!-- Company Info Style -->
        <Style x:Key="CompanyInfoStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <!-- Performance Text Style -->
        <Style x:Key="PerformanceTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>

        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="5, 0, -5,0 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="0, 5, 0,-5 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" Height="{TemplateBinding Height}" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1,0"/>
            <Setter Property="Width" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="MinWidth" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.02*"/>
                                <RowDefinition Height="4*"/>
                                <RowDefinition Height="0.02*"/>
                            </Grid.RowDefinitions>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.Row="1"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="1">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbVertical}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="MinHeight" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="BorderThickness" Value="0,1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.02*"/>
                                        <ColumnDefinition Width="4*"/>
                                        <ColumnDefinition Width="0.02*"/>
                                    </Grid.ColumnDefinitions>
                                    <Track x:Name="PART_Track" Grid.Column="1" IsEnabled="{TemplateBinding IsMouseOver}">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbHorizontal}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanHorizontallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" CanVerticallyScroll="False" Grid.Column="0" Content="{TemplateBinding Content}" CanContentScroll="{TemplateBinding CanContentScroll}" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Background="{DynamicResource White}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Профіль авіакомпанії" 
                               Style="{StaticResource TitleTextStyle}"/>
                    <TextBlock x:Name="CompanyNameText" 
                               Text="Завантаження..."
                               FontFamily="Bahnschrift"
                               FontSize="18"
                               Foreground="{DynamicResource HintColor}"/>
                </StackPanel>

                <Border Grid.Column="1" 
                        Background="{DynamicResource White}"
                        CornerRadius="50"
                        Width="80" Height="80"
                        BorderThickness="3"
                        BorderBrush="{DynamicResource MainColor20}">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" 
                                        ShadowDepth="2" 
                                        Color="{DynamicResource Shadow}" 
                                        Opacity="0.1"/>
                    </Border.Effect>
                    <Ellipse Fill="{DynamicResource MainColor100}" 
                             Margin="15"/>
                </Border>
            </Grid>

            <!-- Quick Stats Row -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Aircraft -->
                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="20" VerticalAlignment="Center">
                        <Ellipse Width="50" Height="50" 
                                Fill="{DynamicResource MainColor100}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1" 
                                                Color="{DynamicResource Shadow}" Opacity="0.2"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="TotalAircraftText" 
                                   Text="0" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="Літаків" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Total Routes -->
                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="20" VerticalAlignment="Center">
                        <Ellipse Width="50" Height="50" 
                                Fill="{DynamicResource MainColor50}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1" 
                                                Color="{DynamicResource Shadow}" Opacity="0.2"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="TotalRoutesText" 
                                   Text="0" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="Маршрутів" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Total Flights -->
                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="20" VerticalAlignment="Center">
                        <Ellipse Width="50" Height="50" 
                                Fill="{DynamicResource MainColor30}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1" 
                                                Color="{DynamicResource Shadow}" Opacity="0.2"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="TotalFlightsText" 
                                   Text="0" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="Польотів" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Active Flights -->
                <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="20" VerticalAlignment="Center">
                        <Ellipse Width="50" Height="50" 
                                Fill="{DynamicResource EnterColor}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1" 
                                                Color="{DynamicResource Shadow}" Opacity="0.2"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="ActiveFlightsText" 
                                   Text="0" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="Активних" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Performance and Details Row -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Flight Performance -->
                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="25">
                        <TextBlock Text="Ефективність польотів" 
                                   Style="{StaticResource CardHeaderStyle}"/>

                        <Grid Margin="0,15,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Completed Flights -->
                            <Grid Grid.Row="0" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Завершено польотів" 
                                               Style="{StaticResource PerformanceTextStyle}"
                                               HorizontalAlignment="Left"/>
                                    <ProgressBar x:Name="CompletedFlightsProgress"
                                                Height="8" 
                                                Background="{DynamicResource MainColor20}"
                                                Foreground="{DynamicResource EnterColor}"
                                                Value="0" Maximum="100"
                                                Margin="0,5,10,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" 
                                           x:Name="CompletedFlightsText"
                                           Text="0"
                                           FontFamily="Bahnschrift"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Foreground="{DynamicResource MainColor100}"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Cancelled Flights -->
                            <Grid Grid.Row="1" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Скасовано польотів" 
                                               Style="{StaticResource PerformanceTextStyle}"
                                               HorizontalAlignment="Left"/>
                                    <ProgressBar x:Name="CancelledFlightsProgress"
                                                Height="8" 
                                                Background="{DynamicResource MainColor20}"
                                                Foreground="#FF6B6B"
                                                Value="0" Maximum="100"
                                                Margin="0,5,10,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" 
                                           x:Name="CancelledFlightsText"
                                           Text="0"
                                           FontFamily="Bahnschrift"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Foreground="#FF6B6B"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Success Rate -->
                            <Border Grid.Row="2" 
                                    Background="{DynamicResource MainColor20}"
                                    CornerRadius="8"
                                    Padding="15">
                                <StackPanel>
                                    <TextBlock Text="Рівень успіху" 
                                               FontFamily="Bahnschrift"
                                               FontSize="14"
                                               Foreground="{DynamicResource MainColor100}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="SuccessRateText" 
                                               Text="0%"
                                               FontFamily="Bahnschrift"
                                               FontSize="24"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource MainColor100}"
                                               HorizontalAlignment="Center"
                                               Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Recent Activity & Company Info -->
                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="25">
                        <TextBlock Text="Інформація про компанію" 
                                   Style="{StaticResource CardHeaderStyle}"/>

                        <Grid Margin="0,15,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Company Details -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                                <TextBlock Text="Назва компанії:" 
                                           FontFamily="Bahnschrift"
                                           FontSize="14"
                                           Foreground="{DynamicResource HintColor}"/>
                                <TextBlock x:Name="CompanyNameDetail" 
                                           Text="Завантаження..."
                                           Style="{StaticResource CompanyInfoStyle}"/>

                                <TextBlock Text="Email:" 
                                           FontFamily="Bahnschrift"
                                           FontSize="14"
                                           Foreground="{DynamicResource HintColor}"
                                           Margin="0,10,0,0"/>
                                <TextBlock x:Name="CompanyEmailText" 
                                           Text="Завантаження..."
                                           Style="{StaticResource CompanyInfoStyle}"/>

                                <TextBlock Text="Країна:" 
                                           FontFamily="Bahnschrift"
                                           FontSize="14"
                                           Foreground="{DynamicResource HintColor}"
                                           Margin="0,10,0,0"/>
                                <TextBlock x:Name="CompanyCountryText" 
                                           Text="Завантаження..."
                                           Style="{StaticResource CompanyInfoStyle}"/>
                            </StackPanel>

                            <!-- Recent Statistics -->
                            <Border Grid.Row="1" 
                                    Background="{DynamicResource DatePickerAdditionalBackground}"
                                    CornerRadius="8"
                                    Padding="15"
                                    Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="Статистика за місяць" 
                                               FontFamily="Bahnschrift"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource MainColor100}"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock x:Name="MonthlyFlightsText" 
                                                       Text="0"
                                                       FontFamily="Bahnschrift"
                                                       FontSize="20"
                                                       FontWeight="Bold"
                                                       Foreground="{DynamicResource MainColor100}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="Польотів" 
                                                       FontFamily="Bahnschrift"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource HintColor}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1">
                                            <TextBlock x:Name="MonthlyPassengersText" 
                                                       Text="0"
                                                       FontFamily="Bahnschrift"
                                                       FontSize="20"
                                                       FontWeight="Bold"
                                                       Foreground="{DynamicResource MainColor100}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="Пасажирів" 
                                                       FontFamily="Bahnschrift"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource HintColor}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Quick Actions -->
                            <StackPanel Grid.Row="2">
                                <TextBlock Text="Швидкі дії" 
                                           FontFamily="Bahnschrift"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource MainColor100}"
                                           Margin="0,0,0,10"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" 
                                            Content="Додати літак"
                                            FontFamily="Bahnschrift"
                                            FontSize="12"
                                            Background="{DynamicResource MainColor100}"
                                            Foreground="{DynamicResource White}"
                                            BorderThickness="0"
                                           
                                            Padding="10,8"
                                            Margin="0,0,5,0"
                                            Cursor="Hand"
                                            Click="AddAircraftButton_Click"/>

                                    <Button Grid.Column="1" 
                                            Content="Новий рейс"
                                            FontFamily="Bahnschrift"
                                            FontSize="12"
                                            Background="{DynamicResource MainColor50}"
                                            Foreground="{DynamicResource White}"
                                            BorderThickness="0"
                                      
                                            Padding="10,8"
                                            Margin="5,0,0,0"
                                            Cursor="Hand"
                                            Click="AddFlightButton_Click"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" 
                    Grid.RowSpan="5"
                    Background="#80FFFFFF"
                    Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Ellipse Width="50" Height="50" 
                             Fill="{DynamicResource MainColor100}"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                        Storyboard.TargetProperty="Angle"
                                                        From="0" To="360"
                                                        Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    <TextBlock Text="Завантаження статистики..." 
                               FontFamily="Bahnschrift"
                               FontSize="16"
                               Foreground="{DynamicResource MainColor100}"
                               Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page> 