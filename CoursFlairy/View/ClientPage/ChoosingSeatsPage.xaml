<Page x:Class="CoursFlairy.View.ClientPage.ChoosingSeatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CoursFlairy.View.ClientPage"
      mc:Ignorable="d" xmlns:convertersearch="clr-namespace:CoursFlairy.ViewModel.Converter.SearchPage"
      xmlns:converter="clr-namespace:CoursFlairy.ViewModel.Converter.Bussines"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChoosingSeatsPage" d:DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Border" x:Key="ButtonStyle">
            <Setter Property="Background" Value="{DynamicResource White}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" To="0.6"/>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" To="5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>

                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" To="0.35"/>
                                <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" To="4"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="ButtonStyleT">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="Foreground" Value="{DynamicResource MainColor100}"/>
        </Style>

        <convertersearch:TextToVisibilityConverter x:Key="TextToVisibilityConverter"/>
        <converter:PlaneBackHeightConverter x:Key="PlaneBackHeightConverter"/>
        <converter:PlaneWindHeightConverter x:Key="PlaneWindHeightConverter"/>
        <converter:ToogleButtonColorConvertor x:Key="ToogleButtonColorConvertor"/>
        <converter:BoolToVisiblityConverter x:Key="BoolToVisiblityConverter"/>
        <converter:TextToColorNullConverter x:Key="TextToColorNullConverter"/>
        <converter:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>

        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="5, 0, -5,0 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle Margin="0, 5, 0,-5 " x:Name="rectangle" Fill="{DynamicResource MainColor100}" RadiusX="4" RadiusY="4" Opacity="0.9" Height="{TemplateBinding Height}" SnapsToDevicePixels="True" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1,0"/>
            <Setter Property="Width" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="MinWidth" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.02*"/>
                                <RowDefinition Height="4*"/>
                                <RowDefinition Height="0.02*"/>
                            </Grid.RowDefinitions>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.Row="1"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="1">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbVertical}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="MinHeight" Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                    <Setter Property="BorderThickness" Value="0,1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.02*"/>
                                        <ColumnDefinition Width="4*"/>
                                        <ColumnDefinition Width="0.02*"/>
                                    </Grid.ColumnDefinitions>
                                    <Track x:Name="PART_Track" Grid.Column="1" IsEnabled="{TemplateBinding IsMouseOver}">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbHorizontal}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanHorizontallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" CanVerticallyScroll="False" Grid.Column="0" Content="{TemplateBinding Content}" CanContentScroll="{TemplateBinding CanContentScroll}" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="Transparent" DataContext="{Binding RelativeSource={RelativeSource AncestorLevel=1, AncestorType=Page, Mode=FindAncestor}}">
        <ScrollViewer Name="MainScroll" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <Grid>
                <StackPanel Orientation="Vertical">
                    <Grid Height="200" Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="20*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>

                        <Border Background="White" CornerRadius="10" Grid.Row="1" Grid.Column="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="0.4" Color="{DynamicResource Shadow}" Opacity="0.5"/>
                            </Border.Effect>
                        </Border>

                        <Grid Grid.Row="1" Grid.Column="1" Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2.5*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Flight Details -->
                            <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,20,0">
                                <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                <Run Text="{Binding DepartureCity, StringFormat={}{0:U}}" FontSize="24" FontWeight="Bold"/>
                <Run Text=" - " FontSize="24" FontWeight="Bold"/>
                <Run Text="{Binding ArrivalCity, StringFormat={}{0:U}}" FontSize="24" FontWeight="Bold"/>
                                </TextBlock>
                                <TextBlock HorizontalAlignment="Center" TextAlignment="Center" Margin="0,5,0,0">
                <Run Text="{Binding DepartureIcao, StringFormat={}{0:l}}" FontSize="16" Foreground="#666666"/>
                <Run Text=" - " FontSize="16" Foreground="#666666"/>
                <Run Text="{Binding ArrivalIcao, StringFormat={}{0:l}}" FontSize="16" Foreground="#666666"/>
                                </TextBlock>
                                <Grid Margin="0,10,0,0" HorizontalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Departure Time -->
                                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                        <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                        <Run Text="{Binding DepartureTime, StringFormat={}{0:HH:mm}}" FontSize="20" FontWeight="SemiBold"/>
                                        </TextBlock>
                                        <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                        <Run Text="{Binding DepartureTime, StringFormat={}{0:dd.MM.yyyy}}" FontSize="16" Foreground="#666666"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Arrow or Separator -->
                                    <TextBlock Grid.Column="1" 
                           Text="-" 
                           FontSize="20" 
                           Margin="15,0,15,0" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Foreground="#666666"/>

                                    <!-- Arrival Time -->
                                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                        <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                        <Run Text="{Binding ArrivalTime, StringFormat={}{0:HH:mm}}" FontSize="20" FontWeight="SemiBold"/>
                                        </TextBlock>
                                        <TextBlock HorizontalAlignment="Center" TextAlignment="Center">
                        <Run Text="{Binding ArrivalTime, StringFormat={}{0:dd.MM.yyyy}}" FontSize="16" Foreground="#666666"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>

                            <!-- Person Icons with Seats and Prices -->
                            <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Economy Class -->
                                <Grid Grid.Column="0" Margin="20,0,20,0" Visibility="{Binding EconomySeats, Converter={StaticResource IntToVisibilityConverter}}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" 
                           Text="{Binding EconomySeats}" 
                           FontSize="24" 
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,5"/>

                                    <Path Name="pathec" Grid.Row="1" 
                      Width="65" Height="65" 
                      Opacity="0.7"
                      Data="{DynamicResource adult 1}" 
                      Fill="{DynamicResource EconomColor}"
                      Stretch="Uniform"
                      StrokeThickness="1.1"
                      Margin="0,0,0,15" 
                      RenderTransformOrigin="0.5,0.5">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="180"/>
                                        </Path.RenderTransform>
                                    </Path>
                                    <Grid Grid.Row="1" Background="Transparent" MouseDown="EconomyPath_MouseDown"/>

                                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" TextAlignment="Center">
                    <Run Text="{Binding EconomyPrice, StringFormat={}{0:N0}}" 
                         FontSize="22"
                         FontFamily="Bahnschrift"/>
                    <Run Text=" грн" 
                         FontSize="22"
                         FontWeight="Medium"
                         FontFamily="Bahnschrift"/>
                                    </TextBlock>
                                </Grid>

                                <!-- Business Class -->
                                <Grid Grid.Column="1" Margin="20,0,20,0" Visibility="{Binding BusinessSeats, Converter={StaticResource IntToVisibilityConverter}}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" 
                           Text="{Binding BusinessSeats}" 
                           FontSize="24" 
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,5"/>

                                    <Path Name="pathbi" Grid.Row="1" 
                      Width="65" Height="65" 
                      Opacity="0.7"
                      Data="{DynamicResource adult 2}" 
                      Fill="{DynamicResource BussinessColor}"
                      Stretch="Uniform"
                      StrokeThickness="1.1"
                      Margin="0,0,0,15"
                      RenderTransformOrigin="0.5,0.5">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="180"/>
                                        </Path.RenderTransform>
                                    </Path>
                                    <Grid Grid.Row="1" Background="Transparent" MouseDown="BusinessPath_MouseDown"/>

                                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" TextAlignment="Center">
                    <Run Text="{Binding BusinessPrice, StringFormat={}{0:N0}}" 
                         FontSize="22"
                         FontFamily="Bahnschrift"/>
                    <Run Text=" грн" 
                         FontSize="22"
                         FontWeight="Medium"
                         FontFamily="Bahnschrift"/>
                                    </TextBlock>
                                </Grid>

                                <!-- First Class -->
                                <Grid Grid.Column="2" Margin="20,0,20,0" Visibility="{Binding FirstClassSeats, Converter={StaticResource IntToVisibilityConverter}}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" 
                           Text="{Binding FirstClassSeats}" 
                           FontSize="24" 
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,5"/>

                                    <Path Name="pathfi" Grid.Row="1" 
                      Width="65" Height="65" 
                      Opacity="0.7"
                      Data="{DynamicResource adult 3}" 
                      Fill="{DynamicResource FirstColor}"
                      Stretch="Uniform"
                      StrokeThickness="1.1"
                      Margin="0,0,0,15"
                      RenderTransformOrigin="0.5,0.5">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="180"/>
                                        </Path.RenderTransform>
                                    </Path>
                                    <Grid Grid.Row="1" Background="Transparent" MouseDown="FirstPath_MouseDown"/>

                                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" TextAlignment="Center">
                    <Run Text="{Binding FirstClassPrice, StringFormat={}{0:N0}}" 
                         FontSize="22"
                         FontFamily="Bahnschrift"/>
                    <Run Text=" грн" 
                         FontSize="22"
                         FontWeight="Medium"
                         FontFamily="Bahnschrift"/>
                                    </TextBlock>
                                </Grid>
                            </Grid>

                            <!-- Airline Name -->
                            <TextBlock Grid.Column="2" 
                   Text="{Binding AirlineName}" 
                   FontSize="30" 
                   FontWeight="SemiBold"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Right"
                   Margin="0,0,20,0"/>
                        </Grid>
                    </Grid>

                    <Grid HorizontalAlignment="Center">
                        <ScrollViewer Name="ConstructorScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Grid Background="{DynamicResource White}">
                                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                    <Grid Height="20"/>
                                    <Line Stroke="{DynamicResource MainColor100}" StrokeThickness="2" X2="{Binding ConstructorWidth}" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                                    <ItemsControl Name="Constructor" Background="{DynamicResource EmptyColor}" Width="{Binding ConstructorWidth}" HorizontalAlignment="Center" VerticalAlignment="Center" ItemsSource="{Binding PlaneStructsList}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <ItemsControl ItemsSource="{Binding}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <UniformGrid Columns="{Binding DataContext.ConstructorColumns, RelativeSource={RelativeSource AncestorType=Grid}}"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Width="{Binding WidthElement}" Height="{Binding HeightElement}" >
                                                                <Border Margin="7" CornerRadius="7" Background="{Binding PlaneLayoutColor}" Opacity="0.6"/>
                                                                <Border Margin="7" CornerRadius="7" BorderThickness="2" BorderBrush="{Binding PlaneLayoutColor}"/>

                                                                <Grid MouseLeftButtonDown="Seat_MouseLeftButtonDown" Background="Transparent">
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="1*"/>
                                                                        <RowDefinition Height="1.5*"/>
                                                                        <RowDefinition Height="0.9*"/>
                                                                    </Grid.RowDefinitions>

                                                                    <Viewbox Grid.Row="1">
                                                                        <TextBlock Text="{Binding SeatText}" Foreground="{DynamicResource EmptyColor}" FontFamily="Bahnschrift"/>
                                                                    </Viewbox>
                                                                </Grid>

                                                                <Grid>
                                                                    <Grid.Resources>
                                                                        <Style TargetType="Path">
                                                                            <Setter Property="Stretch" Value="Uniform"/>
                                                                            <Setter Property="Fill" Value="{DynamicResource EnterColor}"/>
                                                                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                                            <Setter Property="Margin" Value="-5"/>
                                                                            <Setter Property="Visibility" Value="{Binding PathVisiblity}"/>
                                                                        </Style>
                                                                    </Grid.Resources>

                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*"/>
                                                                        <RowDefinition Height="*"/>
                                                                        <RowDefinition Height="*"/>
                                                                        <RowDefinition Height="*"/>
                                                                    </Grid.RowDefinitions>

                                                                    <Path Grid.Row="1" Data="{DynamicResource arrow}" RenderTransform="{Binding PathAngleTransform}"/>
                                                                    <Path Grid.Row="2" Data="{DynamicResource arrow}" RenderTransform="{Binding PathAngleTransform}"/>
                                                                </Grid>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Line Stroke="{DynamicResource MainColor100}" StrokeThickness="2" X2="{Binding ConstructorWidth}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,20">
                            <Border x:Name="FilteredButton" 
                                Style="{StaticResource ButtonStyle}"
                                Width="200" Height="50"
                                Margin="0,0,10,0"
                                MouseDown="FilteredButton_MouseDown">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="0.4" Color="{DynamicResource Shadow}" Opacity="0.35"/>
                                </Border.Effect>
                                <Grid>
                                    <Border Background="{DynamicResource MainColor100}" 
                                        CornerRadius="8"
                                        x:Name="FilteredButtonOverlay"
                                        Opacity="1"/>
                                    <TextBlock Text="За фільтром" 
                                         Style="{StaticResource ButtonStyleT}"
                                         FontSize="20"
                                         x:Name="FilteredButtonText"
                                         Foreground="White"/>
                                </Grid>
                            </Border>

                            <Border x:Name="RandomButton" 
                                Style="{StaticResource ButtonStyle}"
                                Width="200" Height="50"
                                MouseDown="RandomButton_MouseDown">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="0.4" Color="{DynamicResource Shadow}" Opacity="0.35"/>
                                </Border.Effect>
                                <Grid>
                                    <Border Background="{DynamicResource MainColor100}" 
                                        CornerRadius="8"
                                        x:Name="RandomButtonOverlay"
                                        Opacity="0"/>
                                    <TextBlock Text="Довільно" 
                                         Style="{StaticResource ButtonStyleT}"
                                         FontSize="20"
                                         x:Name="RandomButtonText"
                                         Foreground="{DynamicResource MainColor100}"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Selected Seats Display -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="1" 
                            Background="White"
                            CornerRadius="8"
                            Margin="0,20,0,0"
                            MinWidth="420"
                            Visibility="{Binding HasSelectedSeats, Converter={StaticResource BoolToVisiblityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="0.4" Color="{DynamicResource Shadow}" Opacity="0.35"/>
                            </Border.Effect>

                            <StackPanel Margin="20,10">
                                <TextBlock Text="Обрані місця:" 
                                     FontSize="18" 
                                     FontWeight="SemiBold" 
                                     Margin="0,0,0,10"
                                     FontFamily="Bahnschrift"
                                     Foreground="{DynamicResource MainColor100}"/>

                                <ItemsControl ItemsSource="{Binding SelectedSeats}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="{Binding WidthElement}" Height="{Binding HeightElement}" >
                                                <Border Margin="7" CornerRadius="7" Background="{Binding OriginalSeatColor}" Opacity="0.6"/>
                                                <Border Margin="7" CornerRadius="7" BorderThickness="2" BorderBrush="{Binding OriginalSeatColor}"/>

                                                <Grid MouseLeftButtonDown="Seat_MouseLeftButtonDown" Background="Transparent">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="1*"/>
                                                        <RowDefinition Height="1.5*"/>
                                                        <RowDefinition Height="0.9*"/>
                                                    </Grid.RowDefinitions>

                                                    <Viewbox Grid.Row="1">
                                                        <TextBlock Text="{Binding SeatText}" Foreground="{DynamicResource EmptyColor}" FontFamily="Bahnschrift"/>
                                                    </Viewbox>
                                                </Grid>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Select Button -->
                        <Border Grid.Row="1" 
                            Grid.Column="1"
                            Background="White"
                            MaxWidth="640"
                            CornerRadius="8"
                            Height="45"
                            Margin="0,15,0,0"
                            Visibility="{Binding HasSelectedSeats, Converter={StaticResource BoolToVisiblityConverter}}"
                            MouseDown="SelectButton_MouseDown">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="0.4" Color="{DynamicResource Shadow}" Opacity="0.35"/>
                            </Border.Effect>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Duration="0:0:0.2" 
                                                                   Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" 
                                                                   To="8"/>
                                                        <DoubleAnimation Duration="0:0:0.2" 
                                                                   Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" 
                                                                   To="0.6"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Duration="0:0:0.2" 
                                                                   Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)" 
                                                                   To="4"/>
                                                        <DoubleAnimation Duration="0:0:0.2" 
                                                                   Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)" 
                                                                   To="0.35"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Обрати" 
                                 FontSize="23"
                                 FontFamily="Bahnschrift"
                                 Foreground="{DynamicResource MainColor100}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <Grid Height="50"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
